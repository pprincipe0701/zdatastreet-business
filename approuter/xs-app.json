{  
    "authenticationMethod": "route",
    "sessionTimeout": 30,
    "logout": {
      "logoutEndpoint": "/do/logout",
      "logoutPage": "/"
    },
    "routes": [     
      {
        "source": "^/service/(.*)$",
        "destination": "srv-binding",
        "authenticationType": "xsuaa"
      }
    ]
  }